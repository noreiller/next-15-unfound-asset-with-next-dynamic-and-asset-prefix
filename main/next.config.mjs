/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // Comment this line to see the right behavior
  assetPrefix: "http://localhost:4000/asset-prefix",
  reactStrictMode: true,
};

export default nextConfig;
