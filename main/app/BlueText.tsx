import { FunctionComponent } from "react";

import styles from "./BlueText.module.css";

export const BlueText: FunctionComponent = () => (
  <p className={styles.blueText}>This text should be blue.</p>
);
